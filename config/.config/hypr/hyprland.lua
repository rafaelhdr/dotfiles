-- Entrypoint only. Each concern lives in its own module below,
-- see AGENTS.md for the file map and common-task pointers.

require("env")
require("monitors")
require("appearance")
require("input")
require("autostart")
require("keybindings")
require("windowrules")
