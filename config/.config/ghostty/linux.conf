# Linux (GTK) only. Not loaded on macOS.

# Custom tab bar styling (GTK CSS). AppKit/macOS ignores this, which is why it
# lives here rather than in the shared config.
gtk-custom-css = ~/.config/ghostty/tabs.css
